---

- name: delete package cache
  shell: 'arch-chroot {{ storage.mountPath }} pacman -Scc --noconfirm'

- name: remount with discard
  shell: 'mount -o remount,discard {{ storage.mountPath }}'

- name: perform fstrim
  shell: 'fstrim -a'
  ignore_errors: yes

...
